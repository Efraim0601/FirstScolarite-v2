# Fichier généré — éditer Caddyfile.template ou relancer deploy-production.sh
{
	email admin@afbdei.com
}

firstsign.afbdei.com {
	encode gzip zstd

	handle /webhooks/* {
		reverse_proxy payment-service:8080
	}

	handle /api/* {
		reverse_proxy api-gateway:8080
	}

	handle {
		reverse_proxy frontend:80
	}
}
